# Installation

Alto Importmap requires PHP 8.3 or later.

```bash
composer require alto/importmap
```

Composer provides the `vendor/autoload.php` file used by the examples in this documentation.

Continue with [Getting started](getting-started.md).
